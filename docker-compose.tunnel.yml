services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: ai_chat_site_cloudflared
    restart: always
    depends_on:
      - ai-chat-site
    command: tunnel --no-autoupdate run --token ${CLOUDFLARED_TOKEN}
